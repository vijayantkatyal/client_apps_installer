> upload install.zip to server
> extract install.zip
> delete install.zip
> make sure have 777 permission for install folder
> open install/install.php in browser

> sudo apt-get update && sudo apt-get install -y php-curl

installer
- file / folder permissions issue
- curl issue (on installer)
- composer issue (on installer)
- php rewrite module issue